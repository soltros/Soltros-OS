[LightDM]
# Disable logind graphics check for container compatibility
# This prevents LightDM from waiting for graphics devices that may not 
# be properly detected in container environments
logind-check-graphical=false
