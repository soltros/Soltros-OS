[Unit]
Description=Restart WirePlumber on resume from sleep
After=suspend.target

[Service]
Type=oneshot
ExecStart=/usr/bin/systemctl --user restart wireplumber.service

[Install]
WantedBy=suspend.target
